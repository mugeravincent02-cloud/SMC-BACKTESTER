# Vite dev/preview proxy destination. Match the backend PORT.
API_PROXY_TARGET=http://127.0.0.1:5000
# Optional public API address for deployments without a same-origin /api proxy.
# VITE_API_BASE_URL=https://your-api.example/api
